<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="me.haeseok.sts.dao.MoimDetailDAO">
    <insert id="addOne" parameterType="MoimDetailDTO">
        Insert Into moim_detail (no, contents, price, location, online_no, category_no, study_category_no)
        Values (#{no}, #{contents}, #{price}, #{location}, #{onlineNo}, #{categoryNo}, #{studyCategoryNo})
    </insert>
</mapper>
<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="me.haeseok.sts.dao.Member">

    <insert id="addMember" parameterType="MemberDTO" useGeneratedKeys="true" keyProperty="no">
        Insert Into member (email, password, nickname, regdate)
        Values (#{email}, #{password}, #{nickname}, now())
    </insert>

    <select id="readMember" parameterType="long" resultType="memberDTO">
        Select * From member Where no=#{no}
    </select>

    <delete id="dropMember" parameterType="long">
        Delete From member Where no=#{no}
    </delete>
</mapper>
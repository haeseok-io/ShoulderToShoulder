<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="me.haeseok.sts.dao.MoimPlatformDAO">
    <insert id="addOne" parameterType="MoimPlatformDTO">
        Insert Into moim_platform (platform_no, moim_no)
        Values (#{platformNo}, #{moimNo})
    </insert>
</mapper>